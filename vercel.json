{
  "buildCommand": "find . -name 'package.json' -not -path './node_modules/*' -exec sed -i 's/\"link:[^\"]*\"/\"latest\"/g' {} + && rm -rf node_modules package-lock.json yarn.lock .next && pnpm install && next build",
  "installCommand": "pnpm install", // Explicitly set pnpm install here too for clarity, though buildCommand takes precedence for this step.
  "framework": "nextjs",
  "regions": ["cpt1"],
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "env": {
    "PRISMA_GENERATE_SKIP_AUTOINSTALL": "true",
    "SEED_DATABASE": "true"
  }
}
