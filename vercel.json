{
  "buildCommand": "node scripts/fix-links.js && npm install --no-package-lock && npx prisma generate && npx prisma migrate deploy && npm run db:seed:prod && next build",
  "installCommand": "npm install --no-package-lock",
  "framework": "nextjs",
  "regions": ["cpt1"],
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "env": {
    "PRISMA_GENERATE_SKIP_AUTOINSTALL": "true",
    "SEED_DATABASE": "true"
  }
}
