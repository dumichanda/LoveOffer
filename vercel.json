{
  "buildCommand": "rm -rf node_modules package-lock.json yarn.lock pnpm-lock.yaml .next && npm cache clean --force && npm install && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["cpt1"],
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "env": {
    "PRISMA_GENERATE_SKIP_AUTOINSTALL": "true",
    "SEED_DATABASE": "true"
  }
}
